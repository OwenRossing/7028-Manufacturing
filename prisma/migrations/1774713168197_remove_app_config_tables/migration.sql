-- Drop app_robot_numbers and app_team_numbers tables if they exist
-- These tables were removed from the schema and are no longer needed
DROP TABLE IF EXISTS "app_robot_numbers" CASCADE;
DROP TABLE IF EXISTS "app_team_numbers" CASCADE;
